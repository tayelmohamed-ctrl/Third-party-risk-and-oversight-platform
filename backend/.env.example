# --- Database ---
DATABASE_URL="postgresql://mal:mal@localhost:5432/mal_tpro?schema=public"

# --- API ---
PORT=3001
CORS_ORIGIN="http://localhost:5173"

# --- Auth (STUBBED for now) ---
# When AUTH_MODE=stub the API trusts x-user-* headers (dev only).
# Switch to "oidc" and fill the issuer/audience when you wire a real IdP.
AUTH_MODE="stub"
# OIDC_ISSUER=""
# OIDC_AUDIENCE=""
